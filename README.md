# Personal Data Dashboard (Streamlit)

A learning project built with **Python + Pandas + Plotly + Streamlit** to practice:
- data cleaning
- exploratory analysis (EDA)
- interactive dashboarding

## What’s inside
**🏃 Fitness** — steps, sleep, weight trends  
**💳 Spending** — daily spend, categories, top merchants  
**✅ Habits** — completion rates, streaks, weekly heatmap  

## Run locally
```bash
python -m venv .venv
# Windows PowerShell
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
python -m streamlit run app/streamlit_app.py
